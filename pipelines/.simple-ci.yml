---
stages:
  - welcome
  - test

welcome:
  jobs:
    - sayhello

sayhello:
  script:
    - echo "Hello world"

test:
  jobs:
    - linting
    - pytest

linting:
  script:
    - pylint simpleci

pytest:
  inside_docker:
    image: ubuntu
    path: /
  script:
    - cat /etc/os-release
    - ls
    - mkdir IAmANewFolder
    - ls | grep Folder
